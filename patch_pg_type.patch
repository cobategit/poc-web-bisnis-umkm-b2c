--- backend/internal/handler/admin_handler.go
+++ backend/internal/handler/admin_handler.go
@@ -163,7 +163,7 @@
 
 	safeContent := bluemonday.UGCPolicy().Sanitize(r.Content)
 	var id string
-	err := a.DB.QueryRow(c, `INSERT INTO articles(title,slug,excerpt,content,cover_image_url,status,published_at,author_id) VALUES($1,$2,$3,$4,$5,$6,CASE WHEN $6='published' THEN NOW() ELSE NULL END,$7) RETURNING id::text`, r.Title, r.Slug, r.Excerpt, safeContent, r.CoverImageURL, r.Status, claims.Subject).Scan(&id)
+	err := a.DB.QueryRow(c, `INSERT INTO articles(title,slug,excerpt,content,cover_image_url,status,published_at,author_id) VALUES($1,$2,$3,$4,$5,$6,CASE WHEN $6::text='published' THEN NOW() ELSE NULL END,$7) RETURNING id::text`, r.Title, r.Slug, r.Excerpt, safeContent, r.CoverImageURL, r.Status, claims.Subject).Scan(&id)
 	if err != nil {
 		c.JSON(400, gin.H{"message": err.Error()})
 		return
@@ -178,7 +178,7 @@
 		return
 	}
 	safeContent := bluemonday.UGCPolicy().Sanitize(r.Content)
-	ct, err := a.DB.Exec(c, `UPDATE articles SET title=$1,slug=$2,excerpt=$3,content=$4,cover_image_url=$5,status=$6,published_at=CASE WHEN $6='published' THEN COALESCE(published_at,NOW()) ELSE NULL END,updated_at=NOW() WHERE id=$7`, r.Title, r.Slug, r.Excerpt, safeContent, r.CoverImageURL, r.Status, c.Param("id"))
+	ct, err := a.DB.Exec(c, `UPDATE articles SET title=$1,slug=$2,excerpt=$3,content=$4,cover_image_url=$5,status=$6,published_at=CASE WHEN $6::text='published' THEN COALESCE(published_at,NOW()) ELSE NULL END,updated_at=NOW() WHERE id=$7`, r.Title, r.Slug, r.Excerpt, safeContent, r.CoverImageURL, r.Status, c.Param("id"))
 	if err != nil || ct.RowsAffected() == 0 {
 		c.JSON(400, gin.H{"message": "update failed"})
 		return
