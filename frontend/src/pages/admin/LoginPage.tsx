import { useState } from 'react'
import { Navigate, useNavigate } from 'react-router-dom'
import { useAuth } from '../../auth/AuthContext'

export function LoginPage(){const {login,user}=useAuth();const navigate=useNavigate();const [email,setEmail]=useState('admin@printku.local');const [password,setPassword]=useState('Admin123!change');const [error,setError]=useState('');const [loading,setLoading]=useState(false);if(user)return <Navigate to="/cms" replace/>;return <div className="login-page"><form className="login-card" onSubmit={async e=>{e.preventDefault();setLoading(true);setError('');try{await login(email,password);navigate('/cms')}catch(err){setError(err instanceof Error?err.message:'Login gagal')}finally{setLoading(false)}}}><div className="brand"><span className="brand-mark">P</span><span>PrintKu CMS</span></div><h1>Login CMS</h1><p>Kelola produk, artikel, review, konten, dan user.</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8}/></label><button className="btn" disabled={loading}>{loading?'Memproses...':'Login'}</button>{error&&<p className="error">{error}</p>}<small>Credential default hanya untuk development. Ganti password setelah seed.</small></form></div>}
