import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { apiFetch, type ApiResult } from '../../api/client'
import { useAuth } from '../../auth/AuthContext'
import type { Review } from '../../types'
export function ReviewsAdminPage(){const {accessToken,can}=useAuth();const qc=useQueryClient();const q=useQuery({queryKey:['admin-reviews'],queryFn:()=>apiFetch<ApiResult<Review[]>>('/admin/reviews',{},accessToken),enabled:!!accessToken});const update=useMutation({mutationFn:(r:Review)=>apiFetch(`/admin/reviews/${r.id}`,{method:'PATCH',body:JSON.stringify({is_approved:r.is_approved,is_featured:r.is_featured})},accessToken),onSuccess:()=>void qc.invalidateQueries({queryKey:['admin-reviews']})});const del=useMutation({mutationFn:(id:string)=>apiFetch(`/admin/reviews/${id}`,{method:'DELETE'},accessToken),onSuccess:()=>void qc.invalidateQueries({queryKey:['admin-reviews']})});return <div className="admin-page"><div className="admin-title"><div><span className="eyebrow">CMS</span><h1>Review</h1><p>Moderasi review pelanggan sebelum tampil di website.</p></div></div><div className="review-admin-grid">{q.data?.data.map(r=><div className="panel" key={r.id}><div className="stars">{'★'.repeat(r.rating)}</div><h3>{r.customer_name}</h3><p>{r.message}</p><div className="review-controls"><label className="check"><input disabled={!can('reviews.write')} type="checkbox" checked={!!r.is_approved} onChange={e=>update.mutate({...r,is_approved:e.target.checked})}/> Approved</label><label className="check"><input disabled={!can('reviews.write')} type="checkbox" checked={r.is_featured} onChange={e=>update.mutate({...r,is_featured:e.target.checked})}/> Featured</label>{can('reviews.write')&&<button className="danger-link" onClick={()=>confirm('Hapus review?')&&del.mutate(r.id)}>Hapus</button>}</div></div>)}</div></div>}
